import time
def run(mission_id: int) -> bool:
    print(
        f"Not implemented yet but the code continues in order to check the statemachine "
        f"(mission_id={mission_id})"
    )
    print("waiting 1 second")
    time.sleep(5)
    return False
